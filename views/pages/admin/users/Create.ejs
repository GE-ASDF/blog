<%- include("../../../partials/cabecalho"); %>

<section class="container">
<% if(success.length > 0){ %>
<div id="mensagem-informacional" class="form-group d-flex flex-column">
    <span class="alert m-0 d-flex justify-content-between align-items-center alert-success">
        <p><%= success %></p>
        <button type="button" class="btn btn-light btn-close mx-1"></button>
    </span>
</div>
<% }else if(error.length > 0){ %>
<div id="mensagem-informacional" class="form-group d-flex flex-column">
    <span class="alert m-1 d-flex justify-content-between align-items-center alert-danger">
        <p><%= error %></p>
        <button type="button" class="btn btn-light btn-close mx-1"></button>
    </span>
</div>
<% } %>
<h1 class="title mb-3"> <i class="bi bi-file-earmark-plus"></i> Cadastro de usuários </h1>
<div class="card border text-start bg-dark text-white">
    <div class="card-body">
        <form action="/admin/users/save" method="POST">
            <div class="form-group">
                <label for="" class="title fw-bold fs-5">Nome:</label>
                <input autofocus autocomplete="off" placeholder="Digite o título da categoria" type="text" name="name" class="form-control">
            </div>
            <div class="form-group">
                <label for="" class="title fw-bold fs-5">Email:</label>
                <input autocomplete="off" placeholder="Digite o e-mail" type="email" name="email" class="form-control">
            </div>
            <div class="form-group">
                <label for="" class="title fw-bold fs-5">Senha:</label>
                <input autocomplete="off" placeholder="Digite a senha" type="password" name="password" class="form-control">
            </div>
            <button type="submit" class="btn btn-primary mt-2"> <i class="bi bi-file-earmark-plus"></i> Cadastrar </button>
        </form>
    </div>
</div>

</section>

<%- include("../../../partials/rodape"); %>